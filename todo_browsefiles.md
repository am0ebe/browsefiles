- theme: 

- done view can it always show git if git exists and then just show last 10 lines (as git oneliner )❗
> i tried done view in todo_browsefiles.md and it shows no git. i think thats cause i was in top-level filelist and checked in a diff dir and not in the dir the file was actually in.

- read @weekplan.py and lets think about this together to map out a plan:
	- its bit older so have to decide whats best approach
	- idea is to integrate w caldav calendar. for this we might need new caldav or use existing one.
	- mk new cal❓ subtasks
	

- 🔧 2 mon/verify all 🔗 if broken or not


## backlog 🔵
- regex in find (prepend query w marker 2 switch mode)
- quick add todo (shortcut ➡ append 2 cur file)
- prio system — GTD-style: @z @w @mmdd @yymmdd
- non-txt files: mk list + menu; select+enter ➡ open w `o()`; multi-select (1/all/some) ➡ open md w subl
